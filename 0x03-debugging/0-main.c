#include "holberton.h"

/**
 *main -gives us a random number and tells if it is positive, negative or cero
 *Return: 0 if success
*/
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
